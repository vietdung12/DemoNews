@model News.ViewModel.Catalog.Image.SetDefaultImageRequest
@{
    ViewData["Title"] = "SetImage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>SetImage</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="SetImage">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ProductId" />
            @for (var i = 0; i < Model.Images.Count; i++)
            {
                <div class="form-group">
                    <label>
                        <img src="https://localhost:5001/user-content/@Model.Images[i].Name" width="60" height="60">
                        <input type="checkbox" asp-for="Images[i].Selected" />
                        <input type="hidden" asp-for="Images[i].Id" />
                        <input type="hidden" asp-for="Images[i].Name" />
                    </label>
                </div>
            }
            <div class="form-group">
                <input type="submit" value="Lưu lại" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
